version: '3'
services:
  web:
    image: beyondlimits99/php:7.0
    hostname: docker
    env_file:
      - .docker.env
    environment:
      CONTAINER_ROLE: app # [app, queue, scheduler]
      APP_ENV: local # [local, staging, production]
    volumes:
      - ./app:/var/www/html
    ports:
      - 7000:80 # Web
      - 7090:9100 # Prometheus, node_exporter
    tty: true
volumes:
  dbdata:
